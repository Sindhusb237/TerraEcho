TerraEcho — Soil Sound Intelligence
=================================

Run the Streamlit interface:

```bash
pip install -r requirements.txt
streamlit run app.py
```

The interface uses a green/brown theme and allows uploading soil tapping audio, runs analysis, shows results, generates a PDF report, and saves metadata locally in `terraecho.db`.
